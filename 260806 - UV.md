오늘은 python 진도를 나갔다. 첫시간이라 실습환경 구축하면서 가상환경을 사용하는 이유에 대해 배웠다. 프로젝트 설정을 공유하기 쉬워지고 의존성 충돌 문제를 해결할 수 있다.

내 집 컴퓨터에는 전역으로 python을 깔았는데..
설정을 다시 해야겠다.

git init처럼 UV로 uv init --python 3.13으로 환경을 설정했다. 이후 uv add ipykernel 를 추가하고 VS code에서 python과 jupyter notebook을 다운로드 했다.

환경을 설정했다고 바로 사용할 수 있는게 아니였는데 .venv 폴더가 만들어져야 그 안에 activate로 가상환경을 실행할 수 있었다.

venv 폴더는 uv run, uv add, uv sync 할 때 자동으로 생성된다.

.venv\Scripts\activate 로 가상환경을 실행해봤다.

실습을 따라하고 git push를 해야했는데 .venv 같은 설정 파일은 push를 하지 않아서 .gitignore 파일을 만든 후 그안에 .venv를 추가했다.

버전에는 major, minor, patch가 있다.

이 설명이 재밌었는데, major은 호환성이 깨지는 변경, minor은 호환 가능한 기능을 추가, patch는 호환 가능한 bug fix를 의미한다.

그런데 파이썬은 minor가 바뀌는데 호환성이 깨지는 경우가 있다고 설명해주셨다.