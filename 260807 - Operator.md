오늘은 연산자를 배웠다.

### 사칙연산 및 특수 산술 연산자
기본 사칙연산 +, -, *, /

// - 몫만 남긴다.

% - 나머지만 남긴다.

** - 거듭제곱

### 복합 대입 연산자
위에서 작성한 연산자 왼쪽으로 (=) 기호를 추가하면 복합 대입 연산자가 된다.

ex) +=, -=, *=, /=, //=, %=, **=

### 단락 회로 평가
이 개념이 신선했다.

and 연산은 좌측이 False면 우측을 연산하지 않고 False를 반환한다.

or 연산은 좌측이 True면 우측을 연산하지 않고 True를 반환한다.

### 동등 및 대소 비교 연산자

#### 동등 비교 연산자

== - 두 값이 서로 같다

!= - 두 값이 서로 다르다

#### 대소 비교 연산자

\< - 좌측이 더 작다

\> - 좌측이 더 크다

<= - 좌측이 더 작거나 같다

\>= - 좌측이 더 크거나 같다