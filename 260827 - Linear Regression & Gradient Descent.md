선형 회귀 - 원인과 결과 사이에 비례하는 규칙이 있다고 가정하고, 데이터를 가장 잘 나타내는 직선을 그어 예측하는 기법

파라미터(parameter) w,b - 직선의 기울기 가중치(weight)와 기본값 편향(bias). 모델이 오차를 최소화하도록 스스로 찾아내는 두 숫자

오차(Error) / 손실함수(Loss function) / 손실값(Loss value) - 모델 예측값과 실제값의 차이가 '오차'. 예측함수의 오차를 정의한 것이 '손실함수'. 손실함수의 결과를 '손실값'

MSE(Mean Squared Error) - 오차를 제곱 후 평균을 낸다. 큰 오차에 더 큰 패널티를 준다. 곡선을 미분 가능

MAE(Mean Absolute Error) - 오차의 절대값을 평균을 낸다. 이상치에 둔감함

경사하강법(Gradient Descent) - 손실이 작아지는 방향으로 가중치를 반복해서 수정하는 최적화 방법

기울기(Gradient) - 가중치가 변할 때 손실이 어느 방향으로 얼마나 변하는지 나타내는 값

학습률(Learning Rate) - 한 번에 얼마나 크게 업데이트할지 결정하는 하이퍼파라미터

수렴(Convergence) - 반복 업데이트를 통해 손실이 점점 작아지다가 더 이상 변하지 않고 일정한 값에 가까워지는 상태

오버슈팅(OverShooting) - 학습률이 너무 커서 최적점에서 멈추지 않고 계속 왔다갔다 하는 현상

지역 최솟값(Local Minimum) - 전체에서 가장 낮은 값은 아니지만, 주변보다는 낮은 값

모델 파라미터(Model Parameters) - 모델이 학습을 통해 직접 찾아낸 값. 보통 '가중치(weights)'와 '편향(bias)'을 의미

하이퍼파라미터(Hyperparameter) - 학습률, 에포크 수 처럼 사람이 직접 설정해야 하는 값

MinMaxScaler - 0~1 사이로 값을 변환

StandardScaler - 평균 0, 표준편차 1로 변환

분산(Variance) - 데이터가 퍼져 있는 정도를 나타내는 값

표준편차(Standard Deviation) - 분산의 제곱근으로, 평균과의 차이를 표준화한 값

과적합(Overfitting) - 훈련 데이터에 너무 잘 맞춰져서 새로운 데이터에 대해 예측 성능이 떨어지는 현상

과소적합(Underfitting) - 훈련 데이터에도 잘 맞지 않고, 새로운 데이터에 대해서도 예측 성능이 떨어지는 현상

일반화(Generalization) - 훈련 데이터에서 학습한 패턴을 새로운 데이터에도 잘 적용하여 예측하는 능력